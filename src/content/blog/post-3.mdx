---
title: "Groth16 Verifier equation proof"
author: "Astro Learner"
subtitle: "Can you cheat the verifier?"
description: "lets understand how groth16 verifier works methamatically"
pubDate: 2026-03-10
tags: ["coding", "python", "typescript"]
---

> [!abstract]- proof of $e(\pi_L, \pi_R) = e(g_1^{\alpha}, g_2^{\beta})e(\pi_{\text{io}},g_2^{\gamma})e(\pi_O,g_2^{\delta})$
> This is exactly what you need for your Obsidian vault to prove *why* the cryptography works. We are going to strip away the elliptic curves and look purely at what happens inside the exponents during the pairings.
> 
> Here is the complete, step-by-step mathematical proof of the Groth16 Verifier equation.
> 
> ### 1. The Setup (Defining the Exponents)
> 
> To keep the algebra clean, let's define $L$, $R$, and $O$ as the clean, evaluated sums of the circuit wires before any toxic waste is added:
> 
> * $L = \sum_{i=0}^n z_i\ell_i(\tau)$
> * $R = \sum_{i=0}^n z_ir_i(\tau)$
> * $O = \sum_{i=0}^n z_io_i(\tau)$
> 
> Now, let's write out the exact exponents of the Prover's three points. Notice that the $\ell$ and $r$ in the $\pi_O$ noise cleanup actually represent the **entire blinded exponents** of the left and right wires:
> 
> * **$\pi_L$ exponent:** $\ell = \alpha + L + \delta_L\delta$
> * **$\pi_R$ exponent:** $r = \beta + R + \delta_R\delta$
> * **$\pi_O$ exponent:** $\frac{Q_{\text{mid}} + h(\tau)z(\tau)}{\delta} + \ell\delta_R + r\delta_L - \delta_L\delta_R\delta$
> 
> ---
> 
> ### 2. Expanding the Left Side (LHS)
> 
> The left side of the Verifier's equation is a single pairing: $e(\pi_L, \pi_R)$.
> Because pairings multiply exponents together, we must multiply the $\ell$ exponent by the $r$ exponent using FOIL.
> 
> $$\text{LHS Exponent} = (\alpha + L + \delta_L\delta) \times (\beta + R + \delta_R\delta)$$
> 
> Let's carefully cross-multiply every single term:
> 
> $$\text{LHS} = \underbrace{\alpha\beta + \alpha R + \alpha\delta_R\delta}_{\text{from } \alpha} + \underbrace{\beta L + L R + L\delta_R\delta}_{\text{from } L} + \underbrace{\beta\delta_L\delta + R\delta_L\delta + \delta_L\delta_R\delta^2}_{\text{from } \delta_L\delta}$$
> 
> Let's group this massive equation into three distinct parts: the anchors, the circuit, and the noise (everything attached to a $\delta$).
> 
> $$\text{LHS} = \alpha\beta + (\beta L + \alpha R) + L R + \delta \Big(\alpha\delta_R + L\delta_R + \beta\delta_L + R\delta_L + \delta_L\delta_R\delta \Big)$$
> 
> ---
> 
> ### 3. Expanding the Right Side (RHS)
> 
> The right side of the Verifier's equation consists of three pairings multiplied together: $e(g_1^{\alpha}, g_2^{\beta}) \cdot e(\pi_{\text{io}}, g_2^{\gamma}) \cdot e(\pi_O, g_2^{\delta})$.
> Because multiplying pairings adds their exponents, we just add the three resulting exponents together.
> 
> **Pairing 1 (The Anchors):**
> 
> 
> $$e(g_1^\alpha, g_2^\beta) \implies \alpha\beta$$
> 
> **Pairing 2 (The Public Inputs):**
> The $\pi_{\text{io}}$ exponent is $\frac{Q_{\text{io}}}{\gamma}$. The pairing multiplies it by $\gamma$, canceling the fraction:
> 
> 
> $$e(\pi_{\text{io}}, g_2^\gamma) \implies \left(\frac{Q_{\text{io}}}{\gamma}\right) \times \gamma = Q_{\text{io}}$$
> 
> **Pairing 3 (The Private Vault):**
> The pairing multiplies the massive $\pi_O$ exponent by $\delta$, which cancels the $\frac{1}{\delta}$ fraction and multiplies the noise terms by $\delta$:
> 
> 
> $$e(\pi_O, g_2^\delta) \implies Q_{\text{mid}} + h(\tau)z(\tau) + \delta \Big( \ell\delta_R + r\delta_L - \delta_L\delta_R\delta \Big)$$
> 
> **Adding the RHS together:**
> 
> $$\text{RHS Exponent} = \alpha\beta + Q_{\text{io}} + Q_{\text{mid}} + h(\tau)z(\tau) + \delta \Big( \ell\delta_R + r\delta_L - \delta_L\delta_R\delta \Big)$$
> 
> ---
> 
> ### 4. The Final Alignment (Proving LHS = RHS)
> 
> We have our two massive equations. Now, we just apply two core truths of the protocol to watch the right side perfectly transform into the left side.
> 
> **Truth 1: The Wires combine**
> Remember the definition of $Q_{\text{io}}$ and $Q_{\text{mid}}$? If we add the public inputs and the private inputs together, we get the complete master linear combination of the circuit wires:
> 
> 
> $$Q_{\text{io}} + Q_{\text{mid}} = \beta L + \alpha R + O$$
> 
> Let's plug that into our RHS equation:
> 
> $$\text{RHS} = \alpha\beta + \beta L + \alpha R + O + h(\tau)z(\tau) + \delta \Big( \ell\delta_R + r\delta_L - \delta_L\delta_R\delta \Big)$$
> 
> **Truth 2: The Logic Gate ($L \times R = O$)**
> Remember the fundamental QAP equation from the very beginning of your studies? $h(\tau) = \frac{L \times R - O}{z(\tau)}$.
> If we rearrange that, we get:
> 
> 
> $$O + h(\tau)z(\tau) = L R$$
> 
> Let's substitute $L R$ into the RHS equation:
> 
> $$\text{RHS} = \alpha\beta + \beta L + \alpha R + L R + \delta \Big( \ell\delta_R + r\delta_L - \delta_L\delta_R\delta \Big)$$
> 
> **Truth 3: The Noise Cleanup Matches the FOIL Expansion**
> Let's look at the noise block on the RHS: $\ell\delta_R + r\delta_L - \delta_L\delta_R\delta$.
> If we substitute the definitions of $\ell$ ($\alpha + L + \delta_L\delta$) and $r$ ($\beta + R + \delta_R\delta$) into this block:
> 
> $$(\alpha + L + \delta_L\delta)\delta_R + (\beta + R + \delta_R\delta)\delta_L - \delta_L\delta_R\delta$$
> 
> $$= \alpha\delta_R + L\delta_R + \delta_L\delta_R\delta + \beta\delta_L + R\delta_L + \delta_R\delta_L\delta - \delta_L\delta_R\delta$$
> 
> $$= \alpha\delta_R + L\delta_R + \beta\delta_L + R\delta_L + \delta_L\delta_R\delta$$
> 
> Let's plug this perfectly expanded noise block back into the final RHS equation:
> 
> $$\text{RHS} = \alpha\beta + (\beta L + \alpha R) + L R + \delta \Big( \alpha\delta_R + L\delta_R + \beta\delta_L + R\delta_L + \delta_L\delta_R\delta \Big)$$
> 
> ### Q.E.D.
> 
> Look at the final RHS equation. Now scroll up and look at the LHS equation from Step 2.
> 
> **They are mathematically identical.** The pairing proves that the Prover knows a valid set of wires where $L \times R = O$, properly anchored by $\alpha$ and $\beta$, completely scrambled by $\delta$, all verified in a single algebraic stroke.
> 