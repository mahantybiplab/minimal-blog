<section class="relative flex items-center justify-center h-[50vh] overflow-hidden bg-transparent">
  <!-- Soft background glow -->
  <div aria-hidden="true" class="absolute inset-0 flex items-center justify-center">
    <div
      class="w-[50vw] h-[50vh] rounded-full
             bg-[radial-gradient(circle_at_center,rgba(120,120,255,0.06),transparent_75%)]
             blur-[120px]"
    ></div>
  </div>

  <!-- Text -->
  <h1 class="relative z-10 text-center text-5xl sm:text-7xl font-bold text-white leading-tight">
    Curiosity
    <span class="block text-2xl sm:text-3xl font-light text-white/60">Beyond Code</span>
  </h1>
</section>
